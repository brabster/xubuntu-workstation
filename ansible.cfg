[defaults]
# Use a per-user directory under /tmp for remote_tmp to avoid the mode 0700
# warning when the default ~/.ansible/tmp directory does not exist and to
# prevent permission collisions between different users running Ansible.
remote_tmp = /tmp/.ansible-${USER}/tmp
