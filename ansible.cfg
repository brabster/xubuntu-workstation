[defaults]
# Project-level Ansible configuration.
# remote_tmp is left at its default (~/.ansible/tmp); workstation.yml pre-creates
# that directory for the target user to prevent the mode 0700 creation warning.
