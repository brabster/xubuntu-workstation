---
- name: Workstation setup
  hosts: localhost
  become: true
  become_method: ansible.builtin.sudo
  vars_files:
    - ./.vars.yml
    - ./vars.yml
  roles:
    - role: networking
    - role: sudo
    - role: cleanup
    - role: updates
    - role: dev
    - role: chrome-browser
    - role: git
    - role: clamav
    - role: nordvpn
      when: not is_gh_actions
    - role: slack
      when: not is_gh_actions
    - role: vscode
      when: not is_gh_actions
    - role: xfce
      when: not is_gh_actions
    - role: cups
      when: not is_gh_actions
    - role: clipboard
      when: not is_gh_actions
