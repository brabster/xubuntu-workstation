---
- hosts: localhost
  become_method: sudo
  vars_files:
    - ./.vars.yml
    - ./vars.yml
  roles:
  - role: ./roles/sudo
  - role: ./roles/cleanup
  - role: ./roles/updates

  - role: ./roles/nordvpn
    
  - role: ./roles/slack
    
  - role: ./roles/vscode
    
  - role: ./roles/xfce
    when: not CI  # needs X11

  - role: ./roles/dev
  - role: ./roles/chrome-browser
  - role: ./roles/git
  - role: ./roles/clamav
